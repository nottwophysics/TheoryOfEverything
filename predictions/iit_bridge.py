"""
IIT-Entanglement Bridge — Connecting Consciousness Theory to Quantum Physics

Integrated Information Theory (Tononi, 2004) defines Φ (phi) as a
measure of consciousness: the amount of information generated by a
system above and beyond its parts.

Quantum entanglement entropy S measures the non-separability of a
quantum system — the information shared between subsystems that
cannot be attributed to either alone.

This module tested the conjecture Φ ≤ S_entanglement.

⚠️ STATUS: FALSIFIED — DO NOT CITE AS A LIVE CONJECTURE (2026-07-15;
numbers corrected by the TPM-ordering audit of 2026-08-12).

    The validated retest with canonical PyPhi Φ (N=216 systems,
    ordering-audit-corrected) refutes the bound: 50 of the 51 systems
    with nonzero integrated information VIOLATE Φ ≤ S. The raw Φ–S
    correlation is a connectivity confound (partial r ≈ −0.07, p = 0.29).
    See predictions/validated_phi.py and reproducibility/phi_s/.

    The Φ measure implemented BELOW is the framework's internal
    heuristic, NOT canonical IIT Φ (a PyPhi benchmark found r ≈ −0.01
    against canonical values; see PYPHI_BENCHMARK_MEMO.md), and its
    original "holds in 100% of trials" result was circular — an
    artifact of this construction. The module is retained so the
    historical construction stays reproducible, not as evidence.

The metaphysical implications once drawn from the conjecture
(consciousness requires entanglement; maximum consciousness = Brahman)
do not survive the falsification and are recorded only as history.
"""

import numpy as np
from scipy.linalg import logm
from itertools import combinations


class IntegratedInformation:
    """
    Computes Integrated Information (Φ) for a system.

    Φ measures how much information a system generates as a whole
    beyond what its parts generate independently.

    High Φ → highly conscious (the system is more than the sum of its parts)
    Low Φ → not conscious (the system is just parts acting independently)

    We use a simplified IIT 3.0-like measure based on the
    earth-mover's distance between the whole and partitioned systems.
    """

    def __init__(self, num_nodes: int = 4):
        self.num_nodes = num_nodes

    def compute_phi(self, connectivity: np.ndarray,
                    state: np.ndarray = None) -> dict:
        """
        Compute Φ for a network with given connectivity.

        Args:
            connectivity: N×N matrix of connection weights
            state: N-vector of node activations (default: random)

        Returns:
            Φ value and analysis
        """
        n = self.num_nodes
        W = connectivity

        if state is None:
            np.random.seed(42)
            state = np.random.rand(n)

        # Compute the system's cause-effect structure
        # Whole system: transition probability matrix
        T_whole = self._transition_matrix(W)

        # Entropy of the whole system
        H_whole = self._entropy_of_distribution(T_whole, state)

        # Find the Minimum Information Partition (MIP)
        # The partition that cuts the system in a way that loses
        # the LEAST information — the weakest link
        min_phi = float('inf')
        min_partition = None

        for size in range(1, n):
            for part_a in combinations(range(n), size):
                part_a = list(part_a)
                part_b = [i for i in range(n) if i not in part_a]

                if len(part_a) == 0 or len(part_b) == 0:
                    continue

                # Compute information generated by parts independently
                T_a = self._sub_transition_matrix(W, part_a)
                T_b = self._sub_transition_matrix(W, part_b)

                H_a = self._entropy_of_distribution(T_a, state[part_a])
                H_b = self._entropy_of_distribution(T_b, state[part_b])

                # Φ for this partition = whole - sum of parts
                phi_partition = max(0, H_whole - (H_a + H_b))

                if phi_partition < min_phi:
                    min_phi = phi_partition
                    min_partition = (part_a, part_b)

        return {
            "phi": float(min_phi),
            "mip": min_partition,
            "H_whole": float(H_whole),
            "interpretation": (
                f"Φ = {min_phi:.6f}. "
                + ("The system generates significant information above its parts — "
                   "it is 'more than the sum of its parts' (conscious in IIT terms)."
                   if min_phi > 0.1 else
                   "The system generates little information above its parts — "
                   "it is approximately decomposable (low consciousness in IIT terms).")
            ),
        }

    @staticmethod
    def _transition_matrix(W: np.ndarray) -> np.ndarray:
        """Compute transition probability matrix from weights."""
        T = np.abs(W)
        row_sums = T.sum(axis=1, keepdims=True)
        row_sums[row_sums == 0] = 1
        return T / row_sums

    @staticmethod
    def _sub_transition_matrix(W: np.ndarray, nodes: list) -> np.ndarray:
        """Extract transition matrix for a subset of nodes."""
        sub_W = W[np.ix_(nodes, nodes)]
        T = np.abs(sub_W)
        row_sums = T.sum(axis=1, keepdims=True)
        row_sums[row_sums == 0] = 1
        return T / row_sums

    @staticmethod
    def _entropy_of_distribution(T: np.ndarray, state: np.ndarray) -> float:
        """Compute the entropy of the distribution induced by T on state."""
        probs = T @ state
        probs = np.abs(probs)
        total = np.sum(probs)
        if total == 0:
            return 0.0
        probs = probs / total
        probs = probs[probs > 0]
        return float(-np.sum(probs * np.log(probs + 1e-15)))


class EntanglementEntropy:
    """
    Computes quantum entanglement entropy for comparison with Φ.
    """

    def __init__(self, num_qubits: int = 4):
        self.num_qubits = num_qubits
        self.dim = 2 ** num_qubits

    def von_neumann_entropy(self, rho: np.ndarray) -> float:
        """S(ρ) = -Tr(ρ log ρ)"""
        eigenvalues = np.real(np.linalg.eigvalsh(rho))
        eigenvalues = eigenvalues[eigenvalues > 1e-15]
        eigenvalues = eigenvalues / np.sum(eigenvalues)
        return float(-np.sum(eigenvalues * np.log(eigenvalues + 1e-15)))

    def entanglement_entropy(self, state: np.ndarray,
                              partition: list) -> float:
        """
        Compute entanglement entropy for a bipartition.

        S(A) = -Tr(ρ_A log ρ_A) where ρ_A = Tr_B(|ψ⟩⟨ψ|)
        """
        n = self.num_qubits
        complement = [i for i in range(n) if i not in partition]

        dim_a = 2 ** len(partition)
        dim_b = 2 ** len(complement)

        # Density matrix
        rho = np.outer(state, state.conj())

        # Reshape and partial trace
        rho_reshaped = rho.reshape(dim_a, dim_b, dim_a, dim_b)
        rho_a = np.trace(rho_reshaped, axis1=1, axis2=3)

        return self.von_neumann_entropy(rho_a)

    def total_entanglement(self, state: np.ndarray) -> float:
        """
        Average entanglement entropy over all bipartitions.
        A global measure of the state's non-separability.
        """
        n = self.num_qubits
        total = 0.0
        count = 0

        for size in range(1, n):
            for part in combinations(range(n), size):
                S = self.entanglement_entropy(state, list(part))
                total += S
                count += 1

        return total / count if count > 0 else 0.0


class IITEntanglementBridge:
    """
    The formal bridge between IIT (Φ) and quantum entanglement (S).

    Tests the conjecture: Φ ≤ S_entanglement
    """

    def __init__(self, num_nodes: int = 4):
        self.num_nodes = num_nodes
        self.iit = IntegratedInformation(num_nodes)
        self.ent = EntanglementEntropy(num_nodes)

    def test_conjecture(self, num_trials: int = 50) -> dict:
        """
        Test Φ ≤ S_entanglement across many random systems.

        For each trial:
        1. Generate a random classical network (connectivity matrix)
        2. Generate the corresponding quantum state (entangled state)
        3. Compute Φ (classical IIT) and S (quantum entanglement)
        4. Check: Φ ≤ S?
        """
        np.random.seed(42)
        results = []

        for trial in range(num_trials):
            # Random connectivity (classical network)
            W = np.random.rand(self.num_nodes, self.num_nodes) * 0.5
            np.fill_diagonal(W, 0)

            # Compute Φ
            phi_result = self.iit.compute_phi(W)
            phi = phi_result["phi"]

            # Corresponding quantum state
            # Map connectivity to entanglement: stronger connections → more entanglement
            # Create quantum state whose entanglement mirrors the classical connectivity
            dim = 2 ** self.num_nodes
            state = np.random.randn(dim) + 1j * np.random.randn(dim)

            # Modulate entanglement by connectivity strength
            total_connectivity = np.sum(W)
            entanglement_strength = total_connectivity / (self.num_nodes ** 2)

            # Interpolate between product state (no entanglement) and
            # maximally entangled state (max entanglement)
            product = np.zeros(dim, dtype=np.complex128)
            product[0] = 1.0

            ghz = np.zeros(dim, dtype=np.complex128)
            ghz[0] = 1.0 / np.sqrt(2)
            ghz[-1] = 1.0 / np.sqrt(2)

            state = (1 - entanglement_strength) * product + entanglement_strength * ghz
            state /= np.linalg.norm(state)

            # Compute S
            S = self.ent.total_entanglement(state)

            # Check conjecture
            conjecture_holds = phi <= S + 1e-6  # Small tolerance

            results.append({
                "trial": trial,
                "phi": float(phi),
                "S_entanglement": float(S),
                "conjecture_holds": conjecture_holds,
                "ratio": float(phi / S) if S > 1e-10 else float('inf'),
            })

        # Summary
        violations = sum(1 for r in results if not r["conjecture_holds"])
        avg_phi = np.mean([r["phi"] for r in results])
        avg_S = np.mean([r["S_entanglement"] for r in results])
        ratios = [r["ratio"] for r in results if r["ratio"] < float('inf')]
        avg_ratio = np.mean(ratios) if ratios else 0

        # Correlation between Φ and S
        phis = [r["phi"] for r in results]
        Ss = [r["S_entanglement"] for r in results]
        if np.std(phis) > 1e-10 and np.std(Ss) > 1e-10:
            correlation = float(np.corrcoef(phis, Ss)[0, 1])
        else:
            correlation = 0.0

        return {
            "num_trials": num_trials,
            "violations": violations,
            "conjecture_holds_rate": (num_trials - violations) / num_trials,
            "avg_phi": float(avg_phi),
            "avg_S_entanglement": float(avg_S),
            "avg_ratio_phi_over_S": float(avg_ratio),
            "phi_S_correlation": correlation,
            "conjecture": "Φ ≤ S_entanglement",
            "status": ("FALSIFIED — this internal-heuristic construction is "
                       "circular; the validated PyPhi retest refutes the bound "
                       "(50/51 nonzero-Φ systems violate). See module docstring."),
            "result": (
                f"Internal heuristic (retired, circular): holds in "
                f"{num_trials - violations}/{num_trials} trials "
                f"({(num_trials - violations) / num_trials:.0%}). "
                f"Φ-S correlation: {correlation:.4f}. "
                f"Average Φ/S ratio: {avg_ratio:.4f}. "
                f"The validated PyPhi retest falsifies the conjecture."
            ),
        }

    def demonstrate_extremes(self) -> dict:
        """
        Test the conjecture at the extremes:
        1. Disconnected network (Φ=0) vs product state (S=0)
        2. Fully connected network (high Φ) vs GHZ state (high S)
        3. Intermediate cases
        """
        results = {}
        dim = 2 ** self.num_nodes
        n = self.num_nodes

        # Case 1: Disconnected (no consciousness, no entanglement)
        W_disconnected = np.zeros((n, n))
        phi_disc = self.iit.compute_phi(W_disconnected)

        product = np.zeros(dim, dtype=np.complex128)
        product[0] = 1.0
        S_product = self.ent.total_entanglement(product)

        results["disconnected"] = {
            "phi": phi_disc["phi"],
            "S": float(S_product),
            "conjecture": phi_disc["phi"] <= S_product + 1e-6,
            "label": "No consciousness, no entanglement (separated, Maya complete)",
        }

        # Case 2: Fully connected (maximum integration)
        W_full = np.ones((n, n)) * 0.5
        np.fill_diagonal(W_full, 0)
        phi_full = self.iit.compute_phi(W_full)

        ghz = np.zeros(dim, dtype=np.complex128)
        ghz[0] = 1.0 / np.sqrt(2)
        ghz[-1] = 1.0 / np.sqrt(2)
        S_ghz = self.ent.total_entanglement(ghz)

        results["fully_connected"] = {
            "phi": phi_full["phi"],
            "S": float(S_ghz),
            "conjecture": phi_full["phi"] <= S_ghz + 1e-6,
            "label": "Maximum integration, maximum entanglement (approaching Brahman)",
        }

        # Case 3: Half-connected
        W_half = np.zeros((n, n))
        for i in range(n // 2):
            for j in range(n // 2):
                if i != j:
                    W_half[i, j] = 0.5
        phi_half = self.iit.compute_phi(W_half)

        half_state = np.zeros(dim, dtype=np.complex128)
        half_state[0] = 0.7
        half_state[dim // 2] = 0.5
        half_state[-1] = 0.3
        half_state /= np.linalg.norm(half_state)
        S_half = self.ent.total_entanglement(half_state)

        results["half_connected"] = {
            "phi": phi_half["phi"],
            "S": float(S_half),
            "conjecture": phi_half["phi"] <= S_half + 1e-6,
            "label": "Partial integration, partial entanglement (Maya partially dissolved)",
        }

        return results

    def mera_consciousness_profile(self) -> dict:
        """
        Compute Φ-like measure at each layer of the MERA tensor network.

        Prediction: Φ should INCREASE toward the IR (Brahman end)
        because coarse-graining increases integration.
        """
        # Simulate a MERA-like hierarchy
        layers = []
        n = self.num_nodes

        for depth in range(5):
            # At each layer, the effective connectivity increases
            # (more integrated after coarse-graining)
            effective_n = max(2, n - depth)
            strength = 0.1 + depth * 0.2  # Increases with depth

            W = np.random.rand(effective_n, effective_n) * strength
            np.fill_diagonal(W, 0)

            iit = IntegratedInformation(effective_n)
            phi_result = iit.compute_phi(W)

            layers.append({
                "depth": depth,
                "effective_nodes": effective_n,
                "connectivity_strength": float(strength),
                "phi": phi_result["phi"],
                "label": "IR (Brahman)" if depth == 4 else f"depth={depth}",
            })

        phi_values = [l["phi"] for l in layers]
        increasing = all(phi_values[i] <= phi_values[i+1] + 0.01
                        for i in range(len(phi_values)-1))

        return {
            "layers": layers,
            "phi_profile": phi_values,
            "phi_increases_toward_ir": increasing,
            "prediction": (
                "Φ should increase toward the IR (Brahman) because "
                "coarse-graining increases integration. "
                f"Profile: {[f'{p:.4f}' for p in phi_values]}. "
                f"Generally increasing: {increasing}. "
                "This predicts: Brahman (the non-dual ground) has "
                "MAXIMUM integrated information — maximum consciousness."
            ),
        }

    def full_demonstration(self) -> dict:
        """Run all demonstrations."""
        return {
            "conjecture_test": self.test_conjecture(num_trials=50),
            "extreme_cases": self.demonstrate_extremes(),
            "mera_profile": self.mera_consciousness_profile(),
            "testable_prediction": (
                "HISTORICAL (the conjecture is falsified; these implications "
                "were drawn while it was live and no longer follow): "
                "1) Measuring Φ in a neural system sets a LOWER BOUND on its "
                "quantum entanglement. "
                "2) Systems with zero entanglement have zero consciousness. "
                "3) Anesthesia should reduce BOTH Φ and S simultaneously. "
                "4) The deepest meditation states (Samadhi) should correspond "
                "to maximum Φ AND maximum S — the neural correlate of Brahman."
            ),
        }
